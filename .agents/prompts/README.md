# Shared Prompts

No agent-agnostic shared prompt files were found during the import.

Claude commands and roles are stored under `../claude/commands/` and
`../claude/roles/` because those formats are Claude-specific.
